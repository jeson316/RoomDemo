<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.example.room.jeson316.roomdemo">

    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".roomdemo.WordDemoActivity"
                android:label="@string/title_activity_word_demo"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".storagedemo.StorageDemoActivity"/>
        <activity android:name=".servicedemo.ServiceActivity"/>
        <activity
                android:name=".others.LayerListDemoActivity"
                android:theme="@style/Theme.LayerListBackground"
                />
        <activity android:name=".notificationdemo.NotificationActivity"/>

        <service android:name=".servicedemo.LongService"/>
        <service android:name=".servicedemo.BindServce"/>
        <service android:name=".servicedemo.LifeForService"/>
        <service android:name=".servicedemo.MyIntentService"/>
    </application>

</manifest>